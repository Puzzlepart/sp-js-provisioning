# Architecture Decision Records

ADRs capture decisions that should remain understandable after the PR context is gone.

Use an ADR when a change introduces or changes:

- a public API or schema contract
- a persistence or identity strategy
- a dependency or runtime strategy
- a cross-handler behavior
- a deliberate workaround for platform or typing limitations

Start from `../templates/adr.md`.

